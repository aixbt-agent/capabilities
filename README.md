# aixbt-agent/capabilities

Agent-editable source for:

- `skills/`
- `tools/`
- `providers/`

Platform services and extracted apps can consume these capabilities through deployment workflows and generated local copies.
